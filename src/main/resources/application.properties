# Porta dinâmica do Render
server.port=${PORT:8083}
spring.application.name=voting-system-user-service

server.address=0.0.0.0


# Eureka Client
eureka.client.serviceUrl.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:https://voting-system-discovery.onrender.com/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.client.initial-instance-info-replication-interval-seconds=5
eureka.client.registry-fetch-interval-seconds=5

# Timeouts
server.connection-timeout=${SERVER_CONNECTION_TIMEOUT:600s}
server.tomcat.connection-timeout=${SERVER_TOMCAT_TIMEOUT:600000}

# Banco de Dados (Render)
spring.datasource.url=jdbc:postgresql://dpg-d2l6tun5r7bs73dcq26g-a.oregon-postgres.render.com:5432/eleicoes_dmim
spring.datasource.username=user_eleicoes
spring.datasource.password=FfjakX7gJLAcD9AmQkqW9G2uNYuCWPya
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Actuator
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.health.db.enabled=true
management.metrics.tags.application=${spring.application.name}

# Métricas
management.metrics.export.prometheus.enabled=true

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.config-url=/v3/api-docs/swagger-config
springdoc.swagger-ui.url=/v3/api-docs
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.try-it-out-enabled=true

# Logging
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
